module main

imports

  signatures/lib/start-sig
  
  statics/access
  statics/base
  statics/modules

rules

  // No injection explication for top-level sort `Start`.
  programOk : Program
  programOk(Program(prog)) :- {s}
    new s,
    !thisMod[s] in s,
    moduleElemsOk(s, prog).
