module p {
  class A {
    protected internal var x = 42
  }
  class B : public A { 
    public foo(a: A, b : int, c: int) {
      a := new A();
      b := x;
      c := a.x;
      return x
    }
  }
}
